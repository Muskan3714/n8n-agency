

services:
   n8n:
      image: n8nio/n8n
      restart: unless-stopped
      ports:
        - "5678:5678"
      environment:
        - N8N_SECURE_COOKIE=false
        - GENERIC_TIMEZONE=Asia/Dhaka
        - TZ=Asia/Dhaka
      volumes:
        - ./n8n_data:/home/node/.n8n

